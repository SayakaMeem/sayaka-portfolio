# Plain HTML / CSS / JavaScript reference

The production portfolio is the Next.js application at the project root. That version is used for Vercel because it includes React interactivity and server-side API routes.

This folder is included because the project owner requested explicit HTML, CSS, and JavaScript source as well. It is a simplified static reference and is not the production deployment.

Open `index.html` directly in a browser to inspect the static version.
